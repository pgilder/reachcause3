
// 'Modern' browsers (IE8+, use CSS-style selectors)
document.querySelector('.sitelongtitle').innerHTML = 'Feeding Hungry Children';

// Using the jQuery library
$('.sitelongtitle').html('Feeding Hungry Children');

// 'Modern' browsers (IE8+, use CSS-style selectors)
document.querySelector('.siteshorttitle').innerHTML = 'WASF';

// Using the jQuery library
$('.siteshorttitle').html('WASF');

// 'Modern' browsers (IE8+, use CSS-style selectors)
document.querySelector('.siteaddress').innerHTML = 'PO Box 83775 Phoenix, AZ 85071, USA';

// Using the jQuery library
$('.siteaddress').html('PO Box 83775 Phoenix, AZ 85071, USA');

// 'Modern' browsers (IE8+, use CSS-style selectors)
document.querySelector('.sitephone').innerHTML = '+1 (602) 241-2873';

// Using the jQuery library
$('.sitephone').html('+1 (602) 241-2873');

// 'Modern' browsers (IE8+, use CSS-style selectors)
document.querySelector('.siteemail').innerHTML = 'info@feedinghungrychildrennow.org';

// Using the jQuery library
$('.siteemail').html('info@feedinghungrychildrennow.org');

// 'Modern' browsers (IE8+, use CSS-style selectors)
document.querySelector('.siteshortdescription').innerHTML = 'Our mission is to provide a transportation link between national food products donors (who have the food surplus) and the independently operated food banks across the country who distribute this life-saving food to children who need it most with the support of a national network of donors just like you.';

// Using the jQuery library
$('.siteshortdescription').html('Feeding Hungry Children(WASF) is a non profit charity founded by Lon Taylor in 2000. WASF works with the Northern Arizona Food Bank, and the Borderland Food Bank, which serves charities throughout Arizona and America. WASF is an approved 501(c)3 charity listed in publication 78');

// COPY CONTENT
// 'Modern' browsers (IE8+, use CSS-style selectors)
document.querySelector('.product').innerHTML = 'nutritious foods';

// Using the jQuery library
$('.product').html('nutritious foods');

// 'Modern' browsers (IE8+, use CSS-style selectors)
document.querySelector('.producttype').innerHTML = 'nutritious meals';

// Using the jQuery library
$('.producttype').html('nutritious meals');

// 'Modern' browsers (IE8+, use CSS-style selectors)
document.querySelector('.recipient').innerHTML = 'hungry children';

// Using the jQuery library
$('.recipient').html('hungry children');


// 'Modern' browsers (IE8+, use CSS-style selectors)
document.querySelector('.recipientshort').innerHTML = 'children';

// Using the jQuery library
$('.recipientshort').html('children');


// 'Modern' browsers (IE8+, use CSS-style selectors)
document.querySelector('.EIN').innerHTML = '86-1004355';

// Using the jQuery library
$('.EIN').html('86-1004355');


// 'Modern' browsers (IE8+, use CSS-style selectors)
document.querySelector('.purpose').innerHTML = 'Rescue Children';

// Using the jQuery library
$('.purpose').html('Rescue Children');
